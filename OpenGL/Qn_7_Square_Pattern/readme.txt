*************
*   Qn. 7   *
*************

Recreate the following figure by using only squares. The outermost square can be
of any dimension but should be entirely visible within the screen. Note:- Make
the background white and the edges in black. Hint: Identify a specific pattern
that is getting repeated.

https://github.com/afeedh/opengl-snippets/blob/master/assets/square_pattern.png

-----------------
Run the program:
-----------------
    - g++ main.cpp -lglut -lGL -lGLEW -lGLU -o main
    - ./main 1 (for the first pattern)
    - ./main 2 (for the second pattern)